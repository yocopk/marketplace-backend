{
  "scripts": {
    "build": "tsc --noEmit && tsc --outDir build",
    "dev": "npm run build && node build/index.js",
    "start": "nodemon"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^3.1.4",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
